import pytest

pytest.skip(
    "secrets_detect: hook logic not yet implemented -- see sentinel-hook-pack build plan, "
    "migration order step 4 (sandbox verification).",
    allow_module_level=True,
)
