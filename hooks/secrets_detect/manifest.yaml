name: secrets_detect.py
version: 0.1.0
status: stub
fail_mode: open
description: >
  Redacts vendor-style secret tokens (cloud/CI-CD/messaging/payment/AI
  provider patterns) from content about to be written to disk via
  Write/Edit/MultiEdit (redact-in-place through updatedInput), and blocks
  Bash commands whose command string carries an obvious secret-shaped
  token (deny-only -- no output mutation is available at that stage).
